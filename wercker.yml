box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.17"
        theme: liquorice

deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - pheres/git-push@0.4.10:
        gh_token: $GIT_TOKEN
        repo: yash2696/yash2696.github.io
        basedir: public
        branch: master

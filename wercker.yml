box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.17"
        theme: liquorice

deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - ysqi/git-push:
        Gh_oauth: $GIT_TOKEN
        Clean_removed_files: true
        repo: yash2696/yash2696.github.io
        basedir: public
        branch: master

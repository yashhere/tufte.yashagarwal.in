box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.17"
        theme: liquorice

deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - uetchy/gh-pages:
        token: $GIT_TOKEN
        repo: yash2696/yash2696.github.io
        path: public
        branch: master
